# Code

Run the workflow from the repository root Figure_code/. The workflow uses the supplied aggregate tables to reproduce the public versions of analytic results and Figs. 1-5. Each module writes its PNG and SVG outputs to its own Figure/ directory. Expected runtime is a few minutes on a standard desktop.
